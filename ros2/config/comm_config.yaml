# ROS2 communication configuration

# First active channel in this list is used for /controller/joy.
priority:
  - wifi
  - bluetooth

# A channel is ignored when no ControllerData packet arrives within this window.
stale_timeout_ms: 200
